fn main() {
    let rez = [];
    for (let i = 0; i < 2; i = i + 1) {
        for (let j = 0; j < 3; j = j + 1) {
            append(rez, i + j);
        }
    }
    assert(rez[0] == 0);
    assert(rez[1] == 1);
    assert(rez[2] == 2);
    assert(rez[3] == 1);
    assert(rez[4] == 2);
    assert(rez[5] == 3);
    rez = null;

    # Go from 10 to 0
    let i = 10;
    for (i = 10; i > 0; i = i - 1) {}

        assert(i == 0);

        # i is 0 now, go from 0 to 10
        for (; i < 10; i = i + 1) {}

            assert (i == 10);

            let x = ["1", "2", 3, "dalv", true];
            let i = 0;
            let v = {};
            for (; i < len(x); i = i + 1) {
                if (i == 2) {
                    break;
                }
                v[i + "x"] = i;
            }
            assert(x[i] == 3);
            assert(len(v) == 2);

            i = 0;
            for (; i < 10; i = i + 1)
                ;
                assert(i == 10);

                let i = 0;
                for (; i < 4; i = i + 1) {
                    if (i == 2) {
                        continue;
                    }
                    assert(i != 2);
                }

                let i = 0;
                for (; i < 4; i = i + 1) {
                    if (i == 2) {
                        continue;
                    }
                    for (let j = 0; j < 4; j = j + 1) {
                        if (j == 2) {
                            continue;
                        }
                        assert(j != 2);
                    }
                    assert(i != 2);
                }

                # This shouldn't be here, meh
                let i = 0;
                while (i < 4) {
                    i = i + 1;
                    if (i == 2) {continue;}
                        assert(i != 2);
                        i = i + 1;
                    }

                    let i = 0;
                    while (i < 4) {
                        i = i + 1;
                        if (i == 2) {continue;}
                            assert(i != 2);
                            i = i + 1;
                        }

                        let i = 0;
                        while (i < 10) {
                            if (i == 5) {
                                i = i + 1;
                                continue;
                            }
                            assert(i != 5);
                            i = i + 1;
                        }

                        let i = 0;
                        while (i < 4) {
                            i = i + 1;
                            if (i == 2) {continue;}
                                assert(i != 2);
                            }

                            let i = 0;
                            for (; i < 4; i = i + 1) {
                                if (i == 2) {continue;}
                                    assert(i != 2);
                                    i = i + 1;
                                }

                                print ('ok\n');
                            }

